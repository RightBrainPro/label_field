The `LabelField` widget allows to show labels in MaterialDesign style like `TextField` and others do that.

It supports only static final position of the label at the moment, that means it doesn't support floating the label.
